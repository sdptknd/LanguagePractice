1. This is a new initiated console project created using intellij idea's new solution option, chose console app

2. A dotnet solution usually have a `.sln` file outside and multiple project directories at root. Each project directory will have `.csproj` file where we can configure the project, it's dependencies etc.

